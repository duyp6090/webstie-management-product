mixin form-delete-restore(path)
    form(
        action=path
        method="POST"
        id="form-delete-restore"
    )
        .d-flex.align-items-start 
            .form-group 
                select(name="type" class="form-control")
                    option(value="") Chọn hoạt động
                    option(value="delete-forever") Xóa vĩnh viễn
                    option(value="restore") Khôi phục
            .form-group 
                input(
                    type="text"
                    name="ids"
                    value=""
                    class="form-control d-none"
                )
                button(type="submit" class="btn btn-primary") Cập nhật