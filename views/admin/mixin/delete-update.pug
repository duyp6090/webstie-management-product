mixin delete-update(item)
    a(
        href=`/${prefixAdmin}/products/detail/${item._id}`
        class="btn btn-secondary btn-sm"
        data-id=item._id
        button-detail
    ) Chi tiết
    a(
        href=`/${prefixAdmin}/products/edit/${item._id}`
        class="btn btn-warning btn-sm"
        data-id=item._id
        button-updated
    ) Sửa
    button(
        class="btn btn-danger btn-sm ml-1"
        data-id=item._id
        button-delete
    ) Xóa